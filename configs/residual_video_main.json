{
  "train_manifest": "manifests/train.jsonl",
  "val_manifest": "manifests/val.jsonl",
  "output_dir": "outputs/residual_video_main",
  "resolution": 256,
  "num_frames": 8,
  "frame_stride": 1,
  "video_fps": 100,
  "audio_sample_rate": 1000000,
  "train_batch_size": 1,
  "max_train_steps": 30000,
  "learning_rate": 0.0002,
  "adam_weight_decay": 0.0001,
  "mixed_precision": "no",
  "audio_channels": 32,
  "base_channels": 32,
  "residual_scale": 1.0,
  "dropout": 0.0,
  "l1_weight": 0.5,
  "residual_weight": 0.5,
  "temporal_weight": 0.3,
  "edge_weight": 0.05,
  "foreground_weight": 4.0,
  "roi_weight": 1.0,
  "checkpointing_steps": 1000,
  "validation_steps": 500,
  "num_val_batches": 13,
  "seed": 1234
}
